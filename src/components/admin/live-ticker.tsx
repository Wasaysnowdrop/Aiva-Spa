"use client"
import { useAdminFeed } from "./admin-realtime-provider"
export function LiveTicker() { const { events, pendingCount, connection } = useAdminFeed(); return <span className="inline-flex items-center gap-2 rounded-full border border-[#2B3038] px-2.5 py-1 text-[10px] capitalize text-[#8E959F]"><span className={`size-1.5 rounded-full ${connection === "live" ? "bg-[#59C98C]" : "bg-[#D9B956]"}`} />{events.length + pendingCount} events · {connection}</span> }